# Development

The canonical editable source file is:

```text
src/indesign/Emetric.jsx
```

Do not edit generated or versioned distribution files directly. For each alpha, beta, or release build, copy the source file into the appropriate `dist/` folder with a versioned filename.
